<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PardusLockApp"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Login (launcher) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- QR Scan -->
        <activity
            android:name=".ui.scan.QRScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Controller -->
        <activity
            android:name=".ui.controller.ControllerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Admin -->
        <activity
            android:name=".ui.admin.AdminActivity"
            android:exported="false" />

        <!-- Boards -->
        <activity
            android:name=".ui.admin.BoardsActivity"
            android:exported="false" />

        <!-- Dashboard -->
        <activity
            android:name=".ui.admin.DashboardActivity"
            android:exported="false" />

        <!-- Board Select (Admin) -->
        <activity
            android:name=".ui.boardselect.BoardSelectActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>
</manifest>
